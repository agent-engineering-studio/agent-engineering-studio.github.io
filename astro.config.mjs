import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://agent-engineering-studio.github.io',
  // Se il repo NON e' <org>.github.io ma un repo diverso, aggiungi base:
  // base: '/nome-repo',
});
